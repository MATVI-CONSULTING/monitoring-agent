{{- if .Values.httpProbe.enabled }}
apiVersion: v1
kind: ServiceAccount

metadata:
  name: {{ include "chart.fullname" . }}-http-probe
  labels:
    {{- include "chart.labels" . | nindent 4 }}
{{- end }}
