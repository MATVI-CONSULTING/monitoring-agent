apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.fullname" . }}-agent-basic-auth
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  username: {{ .Values.agent.basicAuth.username | quote }}
  password: {{ .Values.agent.basicAuth.password | quote }}
